[package]
name = "frame_send"
version = "0.1.0"
edition = "2021"
description = "Push one closed-loop frame to a shadowgraph device over TCP and select it over UDP"

[dependencies]
clap = { version = "4.6", features = ["derive"] }

[[bin]]
name = "frame_send"
path = "src/main.rs"
